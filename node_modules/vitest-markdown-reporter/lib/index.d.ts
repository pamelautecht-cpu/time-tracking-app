import { Reporter, Vitest, File } from 'vitest';

type ReporterOptions = {
    title?: string;
    outputPath?: string;
    permalinkBaseUrl?: string;
    enableGithubActionsSummary?: boolean;
    flat?: boolean;
};
declare class VitestMarkdownReporter implements Reporter {
    private readonly reporterOptions;
    private log;
    ctx: Vitest;
    private start;
    constructor(reporterOptions?: ReporterOptions);
    onInit(ctx: Vitest): void;
    onFinished: (files?: File[]) => Promise<void>;
}

export { ReporterOptions, VitestMarkdownReporter, VitestMarkdownReporter as default };
